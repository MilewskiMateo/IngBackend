version: "3"
services:
  web-login:
    build:
      context: .
      dockerfile: ./Docker/login/Dockerfile
    ports: ["8080:5000"]
    volumes: [./home_app:/home_app]
    environment:
      FLASK_ENV: development
  redis-db:
    image: "redis:alpine"